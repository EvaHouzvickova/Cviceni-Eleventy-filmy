---
layout: zakladni
pagination:
  data: filmy
  size: 1
  alias: film
permalink: "/film/{{ film.nazev | slug }}/"
---

<h2>{{ film.nazev }}</h2>
<dl>
    <dt>Rok:</dt> 
    <dd>{{ film.rok }}</dd>
    <dt>Země:</dt>
    <dd>{{ film.zeme | join (', ') }}</dd>
    <dt>Žánr:</dt> 
    <dd>{{ film.zanr | join (' / ') }}</dd>
    <dt>Délka:</dt>  
    <dd>{{ film.delka }} minut</dd>
</dl>

<h3>Obsah</h3>
<p>{{ film.obsah.text }} (<a href="{{film.obsah.autor.odkaz}}">{{film.obsah.autor.jmeno }})</a></p>
<img src="{{ film.plakat.url }}" alt="{{ film.nazev }}" width="{{ film.plakat.sirka }}" height="{{ film.plakat.vyska }}">
<h3>Hodnocení</h3>

<h3>Související filmy</h3>
<ul>
    <li><a href="/film/{{ film.nazev | slug }}">{{ film.souvisejici.nazev }}</a></li>
</ul>

